[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Cache headers for wind data
[[headers]]
  for = "/wind/*"
  [headers.values]
    Cache-Control = "public, max-age=3600, stale-while-revalidate=86400"

# Long-lived cache for wind archive files (historical data never changes)
[[headers]]
  for = "/wind/archive/*"
  [headers.values]
    Cache-Control = "public, max-age=86400, stale-while-revalidate=604800"

# Service worker must always be fetched fresh
[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache"

# Manifest served with correct MIME type
[[headers]]
  for = "/manifest.webmanifest"
  [headers.values]
    Content-Type = "application/manifest+json"

# Edge function for dynamic OG meta tags
[[edge_functions]]
  path = "/*"
  function = "og-meta"

# Clean URL for OG images per date (Facebook-friendly, no query params)
[[redirects]]
  from = "/og/*"
  to = "/.netlify/functions/og"
  status = 200

# API functions
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  node_bundler = "esbuild"

[functions.og]
  included_files = [
    "./node_modules/@resvg/resvg-wasm/index_bg.wasm",
    "./netlify/functions/fonts/*",
  ]
  maxDuration = 26